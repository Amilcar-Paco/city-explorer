spring.application.name=city-explorer-backend
# Database connection settings
spring.datasource.url=postgresql://city-explorer_owner:QGpbPrKY7B0u@ep-snowy-boat-a5s4k706.us-east-2.aws.neon.tech/city-explorer?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate specific settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging SQL statements (optional)
spring.jpa.show-sql=true

# If you encounter character encoding issues, you can set this property
# spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci

# Connection pool settings (if needed)
# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.maximum-pool-size=20
# spring.datasource.hikari.idle-timeout=30000
